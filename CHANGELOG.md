# 更新日志

版本号采用 [语义化版本](https://semver.org/lang/zh-CN/)。

## [0.1.0] — 2026-09-11（测试版）

首个可自用的测试版。以「论文 Figure」为主体管理原始数据、图文件与溯源，全部本地运行。

### 新增
- **论文工作区**：新建即建磁盘文件夹（`D:\ResearchData\<code>\`），可归档。
- **Figure / 面板树**：建图即建同名文件夹，支持 Figure → 面板嵌套，磁盘与界面同构。
- **看板 + 详情抽屉**：状态（候选/采用/弃用/待重做）、重要度（关键/一般/辅助）、标签、备注与决策日志。
- **文件管理**：上传（复制入图文件夹并记 SHA-256）、下载、删除；粘贴截图存为预览。
- **图片预览**：可渲染格式直接内联；TIF/TIFF 等浏览器不支持的格式在导入时转成真 PNG 缩略图。
- **溯源**：每张图可查看来源文件、相对路径、SHA-256，反查全链。
- **扫描导入**：指向已有文件夹 → 只读发现图片 → 勾选导入为 Figure（原图留档 + 缩略图）。
- **全局检索**：跨论文按标签 / 文件名 / 样本 / 工作区名检索。
- **投稿导出**：对标记「采用」的节点生成 SourceData 清单 / 图注草稿 / 数据可得性声明。
- **备份**：一键备份 SQLite + 文件清单（CSV）+ 可选整树拷贝，快照防重名。
- **移除**：删除（文件夹移入 `<工作区>/.trash/`，可找回）与隐藏/归档（节点级，可恢复）。
- **一键启动**：`启动工作台.bat`，首次自动建虚拟环境、装依赖、起服务。

### 说明
- 数据库为 SQLite（仅索引与元数据，不存大文件）；文件系统是真源。
- 旧版数据库缺 `archived` 列时启动自动补列（幂等、非破坏）。

### 已知问题
见 [docs/known-issues.md](docs/known-issues.md)。
